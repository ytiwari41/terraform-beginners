# terraform-beginners
This is for beginners

## Recommended commands

Run these to initialize and validate the configuration locally:

```bash
terraform init
terraform plan -out=tfplan
terraform show -json tfplan | jq
```
